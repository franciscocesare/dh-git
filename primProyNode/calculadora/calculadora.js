const sumar = require('./functions/sumar')
const restar = require('./functions/restar')
const multiplicar = require('./functions/multiplicar')
const dividir = require('./functions/dividir')

console.log(sumar(2, 4));
console.log(restar(10, 9));
console.log(multiplicar(2, 4));
console.log(dividir(4, 8));


//que diferencia hay entre llaves o no? porque cambia el color de module.exports